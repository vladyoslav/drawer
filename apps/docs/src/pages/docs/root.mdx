import { Info } from '@/entities/info'
import { Table } from '@/entities/table'

# Root

Contains all the parts of a drawer.

```tsx copy
import { useState } from 'react'

import { Drawer, type Snap } from '@vladyoslav/drawer'

// or import { DrawerRoot } from '@vladyoslav/drawer'

export const MyComponent = () => {
  const snapPoints = ['100px', '50%', '100%']
  const [snap, setSnap] = useState<Snap>(snapPoints[0])
  const [open, setOpen] = useState(false)

  return (
    <Drawer.Root
      // defaultOpen={true} cannot be used with `open` and `onOpenChange`
      open={open}
      onOpenChange={setOpen}
      snapPoints={snapPoints}
      snap={snap}
      setSnap={setSnap}
      modal={false}
      dismissible={false}
      shouldScaleBackground={true}
      scaleFrom={snapPoints[1]}
    >
      ...
    </Drawer.Root>
  )
}
```

## API

The `Root` component takes all props from [`@radix-ui/react-dialog`](https://www.radix-ui.com/primitives/docs/components/dialog#root) `Root` and some additional ones:

<Table>
  | Prop | Type | Default |
  |--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|----------------------------------------------------------|------------|
  | `defaultOpen`
  <Info>
    [`@radix-ui/react-dialog`](https://www.radix-ui.com/primitives/docs/components/dialog#root)
    prop <br />
    <br /> [Read the description](https://www.radix-ui.com/primitives/docs/components/dialog#root)
  </Info>{' '}
  | `boolean` | | | `open`
  <Info>
    [`@radix-ui/react-dialog`](https://www.radix-ui.com/primitives/docs/components/dialog#root)
    prop <br />
    <br /> [Read the description](https://www.radix-ui.com/primitives/docs/components/dialog#root)
  </Info>{' '}
  | `boolean` | | | `onOpenChange`
  <Info>
    [`@radix-ui/react-dialog`](https://www.radix-ui.com/primitives/docs/components/dialog#root)
    prop <br />
    <br /> [Read the description](https://www.radix-ui.com/primitives/docs/components/dialog#root)
  </Info>{' '}
  | `function`<Info>`(open: boolean) => void`</Info> | | | `modal`
  <Info>
    [`@radix-ui/react-dialog`](https://www.radix-ui.com/primitives/docs/components/dialog#root)
    prop <br />
    <br /> [Read the description](https://www.radix-ui.com/primitives/docs/components/dialog#root)
  </Info>{' '}
  | `boolean` | `true` | | `snapPoints`
  <Info>
    Specifies an array of snap points. Should go from the least visible.
    <br />
    <br />
    Example `[100, '200px', '100%']`.
    <br />
    <br />
    You can use `number` (same as `px`), `%` (of the drawer height).
    <br />
    <br />
    **Arithmetic operations are not supported.**
  </Info>{' '}
  | `Snap[]`<Info>`(number \| string)[]`</Info> | `['100%']` | | `snap`
  <Info>
    The controlled snap point state of the drawer.
    <br />
    <br />
    **Must be used in conjunction with `setSnap`.**
  </Info>{' '}
  | `Snap`<Info>`number \| string`</Info> | | | `setSnap`
  <Info>
    Called when the current snap point should change.
    <br />
    <br />
    **Must be used in conjunction with `snap`.**
  </Info>{' '}
  | `SetSnap`<Info>`(snap: number \| string) => void`</Info> | | | `dismissible`
  <Info>
    Sets whether the drawer can be closed by interacting with the background or
    by dragging it down.
  </Info>{' '}
  | `boolean` | `true` | | `scrollLockTimeout`
  <Info>
    Specifies the timeout in `ms` during which the drawer is not draggable after
    scrolling.
  </Info>{' '}
  | `number` | `300` | | `scaleFrom`
  <Info>
    The `y` value from which the component should scale the background.
  </Info>{' '}
  | `number \| string` | `0` | | `shouldScaleBackground`
  <Info>
    Sets whether the element with `vladyoslav-drawer-wrapper` data attribute
    should be scaled.
  </Info>{' '}
  | `boolean` | `false` |
</Table>
